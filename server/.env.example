# Server
PORT=4000

# MySQL connection
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=pronose

# Auth
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# CORS origin for the Vite dev client (only needed if not using the proxy)
CLIENT_ORIGIN=http://localhost:5173
